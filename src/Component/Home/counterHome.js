import { createSlice } from "@reduxjs/toolkit";

const counterHomeSlice = createSlice({
  name: "Home",
  initialState: {},
  reducers: {},
});

export const {} = counterHomeSlice.actions;

export default counterHomeSlice.reducer;
